# Contributing

- Keep changes local and reproducible.
- Add or update tests for every behavioral change.
- Keep CLI behavior deterministic and avoid external calls.

Run:

```bash
PYTHONPATH=src python3 -m unittest discover -s tests
```
