# Changelog

## v0.1.0 - 2026-06-10

- Initial local release of `mcp-tool-risk-audit`.
- Added CLI for MCP manifest audit with markdown/JSON output.
- Added risk checks for commands, filesystem paths, secret-like env vars, network auth gaps, broad scopes, and missing docs/auth hints.
- Added `--format`, `--output`, and `--fail-on`.
- Added GitHub Actions CI and test coverage.
