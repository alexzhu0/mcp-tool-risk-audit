# Security

If you discover a security issue, report it privately first via a minimal reproducible manifest and steps.

This project currently has no external dependencies and no network calls during normal CLI runs.
